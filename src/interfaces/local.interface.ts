export interface LocalEngineOptions {
  /** Base directory on disk where files are stored. */
  baseDir: string;
  /** Public URL prefix used when resolving file links. */
  publicBaseUrl?: string;
}
